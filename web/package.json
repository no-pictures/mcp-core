{
  "name": "mcp-core-ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The mcp-core web-UI shell frontend. Declares the browser dependencies (read by the crate's build.rs to vendor them) and the dev tooling (format/lint/type-check). The shell source lives in src/, which is all the build bakes -- so this manifest is never served.",
  "scripts": {
    "format": "prettier --write \"src/**/*.{ts,scss}\"",
    "format:check": "prettier --check \"src/**/*.{ts,scss}\"",
    "lint": "eslint src --max-warnings 0",
    "lint:fix": "eslint src --fix",
    "typecheck": "tsc --noEmit",
    "check": "npm run format:check && npm run lint && npm run typecheck",
    "test:unit": "vitest run",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "@lit/reactive-element": "^2",
    "bootstrap": "^5",
    "lit": "^3",
    "lit-element": "^4",
    "lit-html": "^3",
    "marked": "^12"
  },
  "devDependencies": {
    "@eslint/js": "^9",
    "@playwright/test": "^1.61.0",
    "eslint": "^9",
    "globals": "^15",
    "prettier": "^3",
    "typescript": "^5",
    "typescript-eslint": "^8",
    "vitest": "^4.1.10"
  }
}
