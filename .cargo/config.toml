# ts-rs writes the web data-contract bindings relative to this base (the `ts-export` feature's
# export test, run by `cargo test`). Only affects cargo commands run inside this crate.
[env]
TS_RS_EXPORT_DIR = { value = "web/src/shell/generated", relative = true }
